# 2026-06-01

## 修复移动端 AI 聊天窗口键盘覆盖问题
- **问题**: 用户点击小智AI后，弹框没弹起 + 输入框被键盘覆盖
- **修复内容** (index.html):
  1. 引入 `visualViewport` API 监听键盘弹出/收起
  2. 移动端键盘弹出时，chat window 切换为 `position: fixed`，动态计算可用高度
  3. 打开聊天时锁定 body 滚动 (`chat-open-mobile`)，防止背景页面滚动
  4. 关闭聊天时恢复 body 滚动和原滚动位置
  5. input focus/blur 自动调整窗口位置
  6. CSS 添加 `transition` 平滑过渡效果

## 修复 AI 聊天 "网络连接异常" 问题
- **根因**: Pages Functions 未正常运行，/api/chat 返回 HTML 而非 JSON，导致 res.json() 抛异常
- **Wrangler CLI 未登录**，无法通过 CLI 部署 Worker/Functions
- **方案**: 前端内嵌本地关键词匹配智能客服 (`localChatReply()`)
  - 覆盖 8 类服务关键词（网页/小程序/iOS/Java/AI/设计/价格/咨询）
  - 支持线索收集（姓名+电话识别 → 提交）
  - AI API 正常时优先使用 AI，不可用时自动降级
  - 通过 GitHub (chuangliu133130/ZhiChuang) push 触发 Cloudflare Pages 自动部署

## 添加线索管理后台 /admin/
- 访问路径: `/admin/`，密码: `admin888`
- 功能: 查看所有线索列表（姓名/电话/需求/来源/时间）、今日/本周统计、自动30秒刷新
- 文件: `public/admin/index.html` 和 `admin/index.html`
- 注意: 依赖 `/api/leads` Pages Function，当前 Cloudflare Pages Functions 未正常，API 数据不可用会显示提示
- 已部署到 CloudStudio (b1f7c99e306240f7ad5fd43a9fddfa70.app.codebuddy.work)
